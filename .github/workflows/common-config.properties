# Required: Deployment target (determines which Kubernetes regions/clusters)
k8s_namespace=es

# Optional: Sidecar configuration
sidecar.enabled=false
sidecar.audit.enabled=false
sidecar.trace.conversion.type=W3C
